<!DOCTYPE html>
<html>
  <head>
    <title>KKApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <header class="bg-cyan-800 p-4 text-white">
      <nav class="flex-col sm:flex-row flex justify-between mx-auto container items-center">
        <div class="my-4 sm:my-0 text-4xl">課題管理表</div>
        <div class="space-x-4 font-bold">
          <% if user_signed_in? %>
            <a href="/" class="hover:text-green-200 transition-all duration-300">プロジェクト一覧</a>
            <%= link_to "ログアウト", destroy_user_session_path, class: "hover:text-green-200 transition-all duration-300", data: { turbo_method: :delete } %>
          <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "hover:text-green-200 transition-all duration-300" %>
            <%= link_to "新規登録", new_user_registration_path, class: "hover:text-green-200 transition-all duration-300" %>
          <% end %>
        </div>
      </nav>
    </header>
    <main class="container mx-auto mt-5 px-5">
      <%= yield %>
    </main>
  </body>
</html>
